# Baby Fall Prevention System Dependencies
# Core Dependencies
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.24.0
opencv-python>=4.8.0

# RT-DETR Detector (Ultralytics)
ultralytics>=8.0.0

# Depth Anything V2 (via Transformers as fallback)
transformers>=4.30.0
Pillow>=10.0.0

# Optional: For direct Depth Anything V2 loading
# timm>=0.9.0
# huggingface_hub>=0.16.0
